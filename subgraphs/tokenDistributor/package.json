{
  "name": "TokenDist",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ vasemkin/token-dist-kovan-v3",
    "create-local": "graph create --node http://localhost:8020/ vasemkin/TokenDist",
    "remove-local": "graph remove --node http://localhost:8020/ vasemkin/TokenDist",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 vasemkin/token-dist-kovan-v3"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
